[WATCHDOG $attempt/$max_attempts]
Missing turn-end signal. Tail: > $promise_quote

Emit ONLY: ` ```tool_calls ` fence (if still working) OR `<final/>` alone (if done).
DO NOT greet, re-summarize, explain, or add any prose. One token response: `<final/>` if done.
