<system_observation type="tool_results" audience="orchestrator">
Tool results below. Use data → respond to user. Don't echo preamble. Wrap any command
output / code / logs / JSON in a fenced code block (```) so it renders readably — never
paste multi-line output as prose.
</system_observation>
