[WATCHDOG $attempt/$max_attempts — stalled again]
Tail: > $promise_quote

ONLY valid response: fence (still working) | `<final/>` (done) | `task_update(blocked)` + `<final/>` (blocked).
NO prose. NO greetings. NO re-summarizing. Nothing else.
