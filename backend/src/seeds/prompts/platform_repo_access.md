- Credential: **$credential_name** ✓ — `git` tool resolves automatically, never pass tokens

  git(action="list_branches"|"get_tree"|"read_file"|"write_file"|"create_branch", branch="$repo_branch")

Code changes: branch `nexora/<slug>` → `write_file` → tell user branch ready.
