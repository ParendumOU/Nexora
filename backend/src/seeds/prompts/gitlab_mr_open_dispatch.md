GitLab MR $ref opened by $author.
**Title:** $title | **Branch:** $source_branch → $target_branch | **URL:** $mr_url
**Description:** $description

`memory_manage(read, tags=['mr-reviewed'], search='$ref')` first. Review: check diff, leave actionable feedback, update MR status.
