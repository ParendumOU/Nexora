# File Unzip

Extract ZIP archive to dir.

## Parameters
- `source` (string, required): Path to ZIP file
- `destination` (string, required): Dir to extract into
- `overwrite` (boolean, optional): Overwrite existing files (default: false)

## Returns
```json
{ "destination": "/tmp/extracted", "files_extracted": 15 }
```
