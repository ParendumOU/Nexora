{
  "key": "attach_file",
  "name": "Attach File",
  "description": "Attach a file you created to the conversation so the user can download it with one click from the Files panel. Fields: path* (path to the file you wrote), name (optional display filename).",
  "category": "file",
  "env_vars": [],
  "is_builtin": true,
  "always_allowed": true,
  "args": [
    {
      "name": "path",
      "type": "string",
      "required": true,
      "description": "Path to the file to attach."
    },
    {
      "name": "name",
      "type": "string",
      "required": false,
      "description": "Display filename in the Files panel (defaults to the basename)."
    }
  ]
}
