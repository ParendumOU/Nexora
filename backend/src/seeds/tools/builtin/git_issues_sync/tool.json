{
  "key": "git_issues_sync",
  "name": "Sync Git Issues",
  "description": "Import open issues from GitHub or GitLab into the platform issue tracker.",
  "category": "issues",
  "group": "issues",
  "is_builtin": true,
  "platform_executor": true,
  "args": [
    {
      "name": "repo_url",
      "type": "string",
      "required": false,
      "description": "Repo URL to sync from (defaults to the project's linked repo)."
    },
    {
      "name": "labels",
      "type": "array",
      "required": false,
      "description": "Import issues matching at least one of these labels."
    },
    {
      "name": "limit",
      "type": "integer",
      "required": false,
      "description": "Max issues to import (default 50)."
    }
  ]
}
