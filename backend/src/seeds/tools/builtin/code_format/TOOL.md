# Format Code

Format code snippet using language-appropriate formatter.

## Parameters
- `code` (string, required): code to format
- `language` (string, required): `python`, `javascript`, `typescript`, `json`, `html`, `css`

## Returns
```json
{ "formatted": "def hello():\n    print('world')\n", "changed": true }
```
